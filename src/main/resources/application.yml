server:
  port: 9090

spring:
  application:
    name: homework
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_jpa_homework
    username: postgres
    password: Huy240419
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    database: postgresql
springdoc:
  swagger-ui:
    tags-sorter: alpha
    doc-expansion: false